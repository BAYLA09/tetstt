export const metadata = { title: "تم استلام طلبك | ليالي بيوتي" };
export default async function ThankYouPage({ params, searchParams }: { params: Promise<{ orderId: string }>; searchParams: Promise<{ total?: string }> }) { const { orderId } = await params; const { total } = await searchParams; return <main className="section"><div className="container card" style={{ padding: 28 }}><span className="badge">طلب ناجح</span><h1>تم استلام طلبك بنجاح</h1><p>طلبك بالدفع عند الاستلام، وسيتواصل معك فريق ليالي بيوتي لتأكيد التفاصيل قبل الشحن.</p><h2>رقم الطلب: {orderId}</h2><p>الإجمالي: AED {total || "--"}</p><ol><li>تأكيد الطلب.</li><li>تجهيز الطلب.</li><li>التوصيل والدفع عند الاستلام.</li></ol><a className="btn secondary" href="/contact">تواصلي مع الدعم</a></div></main>; }
