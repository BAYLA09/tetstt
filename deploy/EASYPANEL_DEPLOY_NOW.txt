## URGENT — ila site kayban "Origin timeout" wla 404

**3lach:** nginx f Docker kan kay9leb `/products/dubai-palace-oud-serum` b7al fichier static (404/timeout).

**Fix sari3 (30s):** EasyPanel → Frontend → **Rollback** l deployment 9bel nginx (8477f88).

**Fix permanent:** Rebuild branch `cursor/production-live-22b5` commit **`c5c2037+`** (nginx fix) + Cloudflare Worker updated.

---

**Branch production (kolchi):** `cursor/production-live-22b5`  
**Commit:** `811e9f0`  
**Live origin daba (qdim):** `8477f88` — Worker v2 kay-maski; rebuild EasyPanel b had branch = fix permanent

---

## FRONTEND (layalibeauty.shop)

| Champ EasyPanel | Valeur |
|-----------------|--------|
| Git branch | `cursor/production-live-22b5` |
| Build context / Root | **repo root** (clone root — EasyPanel default) |
| Dockerfile | `frontend/Dockerfile` |
| Replicas | **1** (mohim — 2 replicas = 50% requests 20s) |

**Build arg mohim** (context = repo root):

```
FRONTEND_SOURCE_DIR=frontend
```

### Build args (copy-paste kol):

```
FRONTEND_SOURCE_DIR=frontend
COMMIT_SHA=811e9f0
BUILD_TIME_UTC=2026-06-04T22:00:00Z
CACHE_BUST=production-live-811e9f0-2
NEXT_PUBLIC_API_BASE_URL=https://api.layalibeauty.shop
NEXT_PUBLIC_ORDER_USE_SAME_ORIGIN_PROXY=false
NEXT_PUBLIC_APP_BUILD_MARKER=layalibeauty-storefront
```

**Alternative:** context = repo root · Dockerfile = `Dockerfile.frontend` (no `FRONTEND_SOURCE_DIR` needed)

**Mohim:** Rebuild + **Disable build cache** / No cache  
Deploy li kay-khlas f 0–2s = restart ghir, ma build-ach

Build logs khasshom ybanu fiha:
- `docker_build_clock_utc=` (date daba)
- `next_build_finished_ok`
- `nginx+next nginx=3000`

---

## BACKEND (api.layalibeauty.shop)

| Champ | Valeur |
|-------|--------|
| Git branch | `cursor/production-live-22b5` |
| Dockerfile | `Dockerfile` (repo root) |

### Build args:

```
COMMIT_SHA=811e9f0
BUILD_TIME_UTC=2026-06-04T22:00:00Z
CACHE_BUST=backend-production-live-811e9f0-1
```

Rebuild + no cache.

---

## Verify mn ba3d deploy

1. Open: https://layalibeauty.shop/api/build-info  
   → `commitSha` khass ybda b **`811e9f0`** (machi `8477f88`)

2. Open: https://layalibeauty.shop/products/dubai-palace-oud-serum  
   → DevTools Network → images **webp** ~67 KB mn **cdn.jsdelivr.net**

3. Page khassha t7ell f **< 3 secondes**

---

## Chno kayn f had branch (811e9f0)

- Swar WebP 67 KB (machi PNG 1 MB)
- Images mn jsDelivr CDN (sri3 bla ma tssena origin)
- nginx f Docker — static files direct
- Cache headers s7a7
- Font wa7ed (Arabic ghir)
- AdClickBeacon deferred (ma kay-blockich page)
- Redirect `/` → serum
- Loading skeleton PDP
- Serum PDP UI: 3 merchant images f scroll (store-ui-refresh)
- Cloudflare Worker v2 (`deploy/cloudflare-worker-fast-images.js`) — deja deployed live

---

## Ila baqi t9ill mn ba3d deploy

1. Frontend replicas = **1**
2. Purge Cloudflare cache
3. (Optional) Cloudflare Worker: `deploy/cloudflare-worker-fast-images.js` route `layalibeauty.shop/*`
